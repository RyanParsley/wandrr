<h1>Listing destinations</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Trip</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @destinations.each do |destination| %>
  <tr>
    <td><%= destination.name %></td>
    <td><%= destinatin.trip.name %></td>
    <td><%= destination.description %></td>
    <% if user_signed_in? && current_user == destination.trip.user%>
      <td><%= link_to 'Edit', edit_trip_destination_path(destination.trip, destination) %></td>
      <!-- <td><%= link_to 'Destroy', trip_destination_path(destination.trip, destination), method:delete, data:{ } %> -->  
  <% end %>
  </tr>
<% end %>
</table>
